# AEO 审核系统产品需求文档 (PRD)

## 1. 项目概述
本系统旨在为企业提供 AEO（高级认证企业）合规审核与资料管理功能。核心目标是通过标准化的路径对应关系和自动化的数据流，确保物理文件管理、CSV 配置与数据库状态的高度统一。

## 2. 核心功能需求

### 2.1 目录结构与数据同步 (单项标准)
- **权威数据源**：以 `audit_list_3.csv` 为唯一事实来源。其 `脱敏文件路径` 必须与物理文件系统路径严格 1:1 对应。
- **“路径即真理”同步逻辑**：
  - 系统需支持根据 CSV 路径自动构建数据库文件夹树（`folders`）。
  - 自动维护闭包表（`folder_closure`），确保多级层级关系正确。
  - **同步自愈**：当 CSV 中出现新路径时，系统应自动创建相关节点，无需人工干预 SQL。
- **命名规范化**：文件夹统一遵循 `序号-名称` 格式（如 `21-1-定位系统车载终端管理制度`），去除冗余分类前缀。

### 2.2 企业业务主体性质过滤 (Customization)
- **业务场景**：企业在系统设置中可选“物流”、“仓储”、“报关”、“外贸”一种或多种性质。
- **动态绑定逻辑**：
  - 系统应根据企业的性质，自动筛选并展示相关的“单项标准”审核项目。
  - **进度计算**：项目的总数（分母）应随绑定项目的增减而动态调整，仅计算合规范围内的进度。

### 2.3 资料管理展示逻辑
- **全路径命名 (Simplified Concatenation)**：
  - 审核项目名称展示应包含完整的层级路径，格式为：`顶级大类名称 - 二级目录名称 - 审核项目文件夹名称`。
  - **原则**：后端仅执行简单的字符串拼接展示，不做任何正则表达式过滤或关键词剔除，确保“所见即所得”。
  - **维护**：名称的准确性由用户通过规范物理文件夹/数据库命名来直接控制。

## 3. 技术架构规范

### 3.1 数据库设计
- 使用 `folders` 存储节点。
- 使用 `folder_closure` 存储树形闭包关系。
- 使用 `folder_check_files` 作为审核项目与文件夹的桥接表。

### 3.2 性能与缓存
- 系统采用 Hyperf 框架，重度依赖缓存。
- **缓存策略**：任何涉及文件夹结构、ID 偏移或分类命名的数据库变更，必须执行 `redis-cli flushall` 以强制清除 JSON 缓存块，并重启后端容器。

## 4. AI 检测与审计规范

### 4.1 关键词检测逻辑 (Keyword Audit)
- **触发条件**：当审核项的 `检测方式` 设置为 `关键词检测` 时触发。
- **PDF 提取范围限制**：为了平衡性能与有效性，系统**仅提取 PDF 文件的前 2 页**作为判定依据。
- **判定标准**：文件内容中必须包含预设的 `检测关键字`。如包含多组关键字（以“、”或“或”分隔），满足其一即视为通过。

### 4.2 核心审核项目 AI 检测与判定对照表

| 业务分类 | 审核项目名称 | 检测方式 | 判定关键字 / 逻辑说明 |
|---|---|---|---|
| **一、加工贸易 (Std 6)** | 一、1 进出口单证复核&保管制度 | 关键词检测 | 包含“单证管理”或“复核” |
| **二、卫生检疫 (Std 6)** | 二、3 特殊物品安全管理制度 | 关键词检测 | 包含“单证管理”或“复核” |
| **三、动植物检疫 (Std 6)** | 三、1 商品检查与进出口法检制度 | 关键词检测 | 包含“商检”或“法检” |
| **五、商品检验 (Std 6)** | 进出口商品检验 、 法检制度 | 关键词检测 | 包含“商检”或“法检” |
| **六、代理报关 (Std 6)** | 六-1 进出口单证及复核制度文件 | 关键词检测 | 包含“进出口单证” |
| **八、物流运输 (Std 6)** | 八-20 运输工具管理制度 | 关键词检测 | 包含“车辆管理” |
| **八、物流运输 (Std 6)** | 八-21-1 运输工具轨迹管理制度 | 关键词检测 | 包含“定位监控”、“轨迹系统”或“车载终端” |
| **八、物流运输 (Std 6)** | 八-22-1 驾驶员安全与匹配制度 | 关键词检测 | 包含“驾驶员审核”、“身份检查”或“人员匹配” |
| **通用-内部控制 (Std 2/4)**| 关企联系合作机制 / 岗位职责 | 关键词检测 | 包含“关企联系”、“贸易安全” |
| **通用-单证管理 (Std 4)** | 进出口单证复核 / 保管制度 | 关键词检测 | 包含“单证复核流程”、“单证归档” |
| **通用-合规审查 (Std 4)** | 禁止类产品合规审查制度 | 关键词检测 | 包含“HS CODE”、“合规审查”、“禁止产品” |
| **通用-财务状况 (Std 1)** | 财务审计报告 | AI 语义理解 | 判定审计意见类型（无保留意见为优） |
| **通用-财务状况 (Std 1)** | 资产负债率情况 | AI 语义理解 | 提取具体负债比率进行指标校验 |
| **通用-守法规范 (Std 5)** | 无犯罪记录声明书 | AI 语义理解 | 判定声明内容是否涵盖关键管理人员 |
| **通用-守法规范 (Std 4)** | 报关单行政处罚合规 | AI 语义理解 | 计算处罚金额累计与报关单违规比例 |

---

## 5. 维护与更新流程
1. **修改源数据**：编辑 `audit_list_3.csv`。
2. **确认物理路径**：确保磁盘上存在对应的 `序号-名称` 文件夹。
3. **执行同步脚本**：运行 `python3 sync_audit_csv_universal.py` 生成 SQL。
4. **数据库应用**：导入 `sync_universal.sql`。
5. **缓存刷新**：执行 `redis-cli flushall` 并重启后端容器（`hyperf-skeleton`）。

---
*Updated by Antigravity - 2026-04-21 (Full AI Detection Schemes Integrated)*
