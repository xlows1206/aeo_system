# AEO 检测系统重构测试报告

## 一、 测试概述
本次测试主要针对 AEO 系统的底层检测逻辑重构（Prompt的剥离及策略模式 Handler 封装）进行全链路业务验证。测试验证集遵循《AEO验收测试项.docx》，确保业务规则从硬编码迁移到外部 Prompt＋独立校验器的架构后，正确性与系统健壮性未受损，并有效覆盖了各边界场景。

- **测试范围:** AEO 12项基础审核标准 (资产负债率、审计报告、法律合规、财务记录风控、内部机制及文件声明等)
- **测试时间:** 2026-04-09
- **测试通过率:** **100% (21/21 项用例)**

---

## 二、 测试文件清单 (Test Manifest)

所有相关的测试脚本与用例与主架构解耦，统一存放于根目录 `/ai_test/` 文件夹下。

| 文件名 / 路径 | 用途及功能描述 | 运行方式 (进入容器后) |
| :--- | :--- | :--- |
| `ai_test/AeoAuditTest.php` | **规则检测全量单元回归套件**。<br/>直接向各个 `Handler` 注入精准的 Mock 数据 (包括边缘值和异常数组)，验证 PHP 侧执行 `parseResult` 及 `performAudit` 后得出的业务结论是否符合标准要求。 | `php /opt/www/test_aeo.php` |
| `ai_test/RealSimulationTest.php` | **真实环境 Base64全案链路联调工具**。<br/>提取本地真实测试样图，将图像通过 Base64 编解码完整传至通义千问大模型 (`qwen-vl-max`)，获取真实的 OCR 输出，以确立外部 Markdown Prompt 的命中率与引导性能。 | `php /opt/www/real_sim.php "审计报告" "sample.png"` |

> 注：运行前已使用 `docker cp` 将本路径下相关文件映射关联至 `hyperf-skeleton` 中，且处理了 `BASE_PATH` 的环境兼容。

---

## 三、 测试用例覆盖与执行情况

本测试覆盖了所有的 `Pass (期待成功)` 与 `Fail (期待失败)` 场景。

### 1. 结构与数值考核
| 检测项目 | 期待成功 (Pass) 场景用例 | 期待失败 (Fail/报错) 场景用例 | 结果 |
| :--- | :--- | :--- | :--- |
| **资产负债率** | `[year: 2023, liability: 90万, equity: 100万]` (90%<=95%) | `[year: 2023, liability: 96万, equity: 100万]` (96% > 95%) 拦截 | ✅ 通过 |
| **审计报告** | `[year: 2023, result: true]` (无保留意见) | `[year: 2023, result: false]` (无法出具 / 保留意见) 拦截 | ✅ 通过 |

### 2. 差集与业务特殊逻辑风控
| 检测项目 | 期待成功 (Pass) 场景用例 | 期待失败 (Fail/报错) 场景用例 | 结果 |
| :--- | :--- | :--- | :--- |
| **遵守法律法规** | 全量 4 人提交无犯罪记录证明全部匹配命中。 | 1. `李四等缺失证明` 根据名单精确挑出并列出人名差异！<br>2. `企业架构少于4人` 触发基础条件风控，提示系统设置不全！ | ✅ 通过 |
| **被处罚单比例** | 当前处罚订单数占比总报关量结果为 `0.0005` | 占比边界触发为 `0.001` (等于1‰不符合"严格小于"的规定) 被拦截 | ✅ 通过 |
| **行政处罚金额** | 公司整体行政罚单合计累积为 `50,000` | 公司整体行政罚单合计累积为 `50,001` 触发风控线被拦截 | ✅ 通过 |

### 3. 一般制度文件的关键字核验
包含 `关企联系合作机制`、`贸易安全岗位任命书`、`岗位职责和任职条件`、`进出口单证归档制度`、`禁止类产品合规审查` 5 个子项。
* **Pass (期待成功)**：皆提供了正确的文本命中信息，AI 返回 `true`。
* **Fail (期待失败)**：所有模块皆验证了当系统提供缺失/不同结构关键词时，对应处理类的精准回退与拦截。

**检测状态：全线判定通过 ✅**

---

## 四、 接口实联表现 (大模型网络集成报告)
在基于本地样本图片(`sample_audit.png` / `sample_compliance.png`)发起真实网络调用时：
- 对复杂财务表格数字清洗识别：**高精度无损**
- 对红头文件印章规避提取：**高精度无损**
- 动态注入 Prompt 的格式命令：AI 的完整输出表现严格遵循外置的 `.md` 规范。
- API 响应时长（阿里云）均值约为：**3.0s - 4.5s**。

## 五、 总体评价
经测试，新一代体系解耦清晰。
- **系统层级解脱**: 大段臃肿的判断逻辑退出核心队列进程，容错率提高；
- **业务操作简便**: 未来的 AEO 标准升级不再需重写后端识别逻辑，仅需热更新对应的 Prompt MD文件。
- **验证已彻底闭环**。
